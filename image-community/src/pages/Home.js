// LIBRARY
import React from 'react';

// COMPONENTS
import ImageCard from '../components/ImageCard';
const Home = (props) => {
  return (
    <React.Fragment>
      <ImageCard />
    </React.Fragment>
  );
};

export default Home;
